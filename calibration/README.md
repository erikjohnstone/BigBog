# Shadow Runtime calibration kit (Gate G-WB)

Generated by `scripts/build_calibration_kit.py` from `bactalk.niagara.calibration`;
do not edit by hand. One folder per ASSUMED rule of `docs/niagara-semantics.md`.
Each holds `program.bog` (import it into a station), `steps.json` (what to do and
when) and one `expected-<point>.csv` per observed history: the trace the Shadow
Runtime produced for the same script.

## Procedure (once, about two hours)

1. Start a clean localhost station with the signed `bactalkG36` module installed
   (Gate G-SDK).
2. For each folder below, in order: import `program.bog` under the station's root folder,
   note the wall-clock time you start the station or enable the folder (this is t=0),
   then perform the steps at the listed times using the point actions in Workbench:
   `override` at level 8 with the listed value, `auto` to remove the override.
3. After `duration_seconds`, export each history under `Outputs/<point>/<point>History`
   as CSV to `gates/evidence/G-WB/<folder>/<point>.csv` (any export with a timestamp
   column
   and a value column is accepted; a status column is used when present).
4. Run `python scripts/calibrate_shadow_runtime.py gates/evidence/G-WB`. It compares the
   exports with the expected traces, prints a verdict per rule and writes VERIFIED or
   CONTRADICTED next to the rule in `docs/niagara-semantics.md`.

The export's clock is aligned to the expected trace (its first record is taken as the
first expected record, then shifted by up to two intervals to the best fit); within that,
a record must lie within 1.5 s of its expected instant and numeric values are compared
to 0.1 % of their range (never below 1e-6). A rule whose observation depends on
an ordering the station does not guarantee (S-LINK-4, S-CLOCK-1) is recorded as the
station's observed behaviour; either outcome is evidence.

## Files

| Folder | Rule | Also covers | Duration | What to look for |
|---|---|---|---|---|
| `s-status-4/` | S-STATUS-4 | — | 20 s | sum reads 5 with ok status, then 2 (b alone) with ok status after a goes null at t=10. |
| `s-status-5/` | S-STATUS-5 | S-LOGIC-2 | 20 s | notOut is true (not false), then after t=10 the writable shows its own fallback true with the in16 null. |
| `s-status-6/` | S-STATUS-6 | S-LINK-2 | 10 s | latched reads 3 from the first history record: the latch clocked at start saw the settled sum. |
| `s-math-2/` | S-MATH-2 | — | 30 s | difference reads 6, then 10 (b null ignored), then the fallback 99 when a is null. |
| `s-math-3/` | S-MATH-3 | S-WRITABLE-5 | 20 s | quotient reads 5 then 1e6 (infinity clamped); quotientStatus's history status shows fault after t=10. |
| `s-cmp-1/` | S-CMP-1 | — | 20 s | greater is true, then after t=10 the writable shows its fallback true with in16 null. |
| `s-latch-2/` | S-LATCH-2 | — | 20 s | latched reads 0 with ok status until t=10, then 7. |
| `s-oneshot-2/` | S-ONESHOT-2 | — | 20 s | The runtime assumes 0.5 s: no one-second history sample should catch the pulse true. |
| `s-oneshot-3/` | S-ONESHOT-3 | — | 20 s | retriggered stays true throughout: the 0.6 s clock keeps restarting the 2 s pulse. |
| `s-mv-2/` | S-MV-2 | — | 40 s | clock is true for the first 10 s, false for the next 10 s, and so on. |
| `s-tstat-2/` | S-TSTAT-2 | S-TSTAT-3 | 20 s | call starts true (cv above the band), then after t=10 shows the fallback true with in16 null. |
| `s-reset-2/` | S-RESET-2 | — | 10 s | rescaled reads 30 and its history status shows fault. |
| `s-loop-2/` | S-LOOP-2 | S-LOOP-4, S-LOOP-5, S-LOOP-6 | 130 s | loopOut changes only at multiples of 10 s (S-LOOP-2), clamps at 100 without winding up (S-LOOP-4), holds when enable drops at t=93 (S-LOOP-5) and shows the fallback -1 once cv is null at t=113 (S-LOOP-6). |
| `s-link-4/` | S-LINK-4 | — | 20 s | glitchLatched becomes true at t=10 if the station propagates each link to completion before the next; it stays false if both links settle before anything downstream runs. |
| `s-link-3/` | S-LINK-3 | — | 30 s | latched rises at t=10, holds through t=15 and clears at t=25; the station log shows no error. |
| `s-clock-1/` | S-CLOCK-1 | — | 20 s | Both delays expire at t=10. tieOrder true means the feed delay fired before the clock delay. |
| `s-sched-3/` | S-SCHED-3 | — | 5 s | Not station-testable: A convention of the runtime, not a Niagara behaviour: no station test. |

## Steps per file

### `s-status-4/` — propagateFlags default: a null input is ignored, no bit propagates

- t=0s: override Inputs/a to 3.0
- t=10s: auto Inputs/a (remove the override)
- Export: `Outputs/sum` after 20 s.

### `s-status-5/` — a null input gives a null output; the fed point falls back (also S-LOGIC-2)

- t=0s: override Inputs/b to False
- t=10s: auto Inputs/b (remove the override)
- Export: `Outputs/notOut` after 20 s.

### `s-status-6/` — start-up: outputs start at zero/ok, links push before the first execution (also S-LINK-2)

- No operator steps: start the station and let the histories fill.
- Export: `Outputs/latched` after 10 s.

### `s-math-2/` — Subtract anchors on inA; a null inB is ignored, a null inA gives null

- t=0s: override Inputs/a to 10.0
- t=0s: override Inputs/b to 4.0
- t=10s: auto Inputs/b (remove the override)
- t=20s: auto Inputs/a (remove the override)
- Export: `Outputs/difference` after 30 s.

### `s-math-3/` — division by zero gives an IEEE result with the fault bit (also S-WRITABLE-5)

- t=10s: override Inputs/b to 0.0
- Export: `Outputs/quotient`, `Outputs/quotientStatus` after 20 s.

### `s-cmp-1/` — a comparison with a null input is null

- t=0s: override Inputs/b to 1.0
- t=10s: auto Inputs/b (remove the override)
- Export: `Outputs/greater` after 20 s.

### `s-latch-2/` — a latch holds the type's zero with ok status before its first clock edge

- t=10s: override Inputs/clock to True
- Export: `Outputs/latched` after 20 s.

### `s-oneshot-2/` — default OneShot pulse width

- t=5s: override Inputs/trigger to True
- t=15s: override Inputs/trigger to False
- Export: `Outputs/pulse` after 20 s.

### `s-oneshot-3/` — a rising edge during a pulse restarts it

- No operator steps: start the station and let the histories fill.
- Export: `Outputs/retriggered` after 20 s.

### `s-mv-2/` — MultiVibrator starts true at station start

- No operator steps: start the station and let the histories fill.
- Export: `Outputs/clock` after 40 s.

### `s-tstat-2/` — Tstat with a null cv is null; it starts false (also S-TSTAT-3)

- t=0s: override Inputs/cv to 25.0
- t=10s: auto Inputs/cv (remove the override)
- Export: `Outputs/call` after 20 s.

### `s-reset-2/` — Reset with equal input limits gives outputLowLimit with the fault bit

- No operator steps: start the station and let the histories fill.
- Export: `Outputs/rescaled` after 10 s.

### `s-loop-2/` — LoopPoint executes on its executeTime clock only (also S-LOOP-4, S-LOOP-5, S-LOOP-6)

- t=0s: override Inputs/cv to 20.0
- t=13s: override Inputs/cv to 25.0
- t=43s: override Inputs/cv to 200.0
- t=73s: override Inputs/cv to 20.0
- t=93s: override Inputs/enable to False
- t=113s: auto Inputs/cv (remove the override)
- Export: `Outputs/loopOut` after 130 s.

### `s-link-4/` — synchronous depth-first propagation makes intermediate values visible

- t=10s: override Inputs/a to 5.0
- Export: `Outputs/glitchLatched` after 20 s.

### `s-link-3/` — a converging feedback loop settles without error

- t=10s: override Inputs/set to True
- t=15s: override Inputs/set to False
- t=25s: override Inputs/notClear to False
- Export: `Outputs/latched` after 30 s.

### `s-clock-1/` — order of two timers due at the same instant

- t=5s: override Inputs/a to True
- Export: `Outputs/tieOrder` after 20 s.

### `s-sched-3/` — runtime convention: simulated time zero is Monday 00:00

- No operator steps: start the station and let the histories fill.
- Export: `Outputs/echo` after 5 s.
