# Integration status

Verified on 2026-09-20. The live machine-readable source is `GET /api/system/readiness`; the UI renders the same ledger. A package being installed is intentionally not treated as production support.

| Layer | Current stage | Evidence / blocker |
| --- | --- | --- |
| Typed BACTalk IR | Verified against pinned OCL vectors | Algebraic and temporal primitives include edge/change detection, delays, latches, moving-window average, sampler, unit delay, reset, and bounded PI. All 137 Open Control Library rules import and pass their upstream scenarios. Stock Niagara lowering covers 113; four temporal primitive families remain on the gated ProgramObject/module target lane. |
| Job/graph boundary | Verified locally | Required points, direction, type, defaults, command authorization, test targets, and output coverage fail closed. |
| Fault and recovery contracts | Product-wired / deterministic | Job acceptance phases can inject eight typed input-fault classes, independently invalidate a quality input, retain raw/effective scan evidence, and prove explicit fallback/recovery behavior. Exhaust proof and pump availability packs exercise the lane. Physics-backed actuator faults, automatic BACnet reliability propagation, and licensed Niagara/runtime qualification remain. |
| Equipment qualification profiles | Product-wired / deterministic | Built-in VAV, AHU, duct-static, exhaust, and pump profiles classify required and conditional normal/safety/failure evidence; contractor profiles can resolve project applicability for custom equipment. The Test Lab shows earned evidence and every open hazard. The matrix is an engineering gate, not a claim of licensed-runtime or field qualification. |
| Contractor document intake | Product-wired | CSV/XLSX point schedules and TXT/Markdown/JSON/DOCX/PDF sequence documents are normalized with bounded parsers. Common BAS aliases and split BACnet object columns map deterministically with a pre-build review; collisions fail closed and source names are preserved. Raw uploads, extracted text, hashes, and generated artifacts are retained under one approval digest. OCR, drawing schedules, arbitrary layouts, and reviewed AI suggestions for unknown names remain. |
| Contractor Niagara environments | Product-wired static/compiler boundary | ZIP packs declare target Niagara version, modules/JARs, redistribution policy, palettes, typed custom component contracts, config-to-property mappings, and graphics assets. Hashes, archive safety, module/type closure, physical slot contracts, parameter presence, and ambiguous target implementations fail closed; exact custom components compile into `.bog` output and all assets join the approval digest. Contractor PX templates compile into signed XML with complete exact-ORD point substitutions; DTDs/entities, scripts, remote/active URIs, unknown points, and missing bindings fail closed. BACTalk never executes uploaded Java or PX. Licensed runtime dependency resolution/import/restart/render behavior remains mandatory. |
| Niagara point binding | Product-wired source/plan boundary | Point CSV/XLSX accepts exact Niagara station ORDs and per-command priorities. Runs emit a signed link plan, readback contract, and deterministic Java installer based on a pinned Apache-2.0 Niagara pattern. It resolves both ends, validates slots, is idempotent for the same link, and rejects target collisions. Generated source compiles against contract stubs; exact licensed-SDK and station execution remain mandatory. |
| Niagara semantic tags / nHaystack | Product-wired source/readback boundary | Shop profiles can declare exact site and equipment ORDs; the generated program root is the default equipment component. Every run emits deterministic Java that installs site/equipment/point `BHDict` annotations, is idempotent for exact matches, and rejects any differing existing dictionary. Expected Zinc readback uses the real shop folder and exact component refs. Compilation plus idempotence/collision behavior pass a Java contract harness; exact nHaystack/Niagara runtime execution and captured cache readback remain mandatory. |
| Niagara station assembly | Target-compiled offline | A run shop profile or whole-project contract explicitly selects compare-only/none, insert, or replace. Insert requires the exact target parent and rejects collisions. Replace requires the exact existing target and refuses deletion when any outside station component refers to a target handle. Generated handles and every internal reference are deterministically rebased; unrelated components are preserved. Multi-equipment assembly is atomic and rejects duplicate target ORDs. Approved exports include the assembled BOG and signed manifest. Exact Workbench import/module resolution/compile/restart/readback remain mandatory. |
| AI custom and library programming | Product-wired / prototype | Unknown equipment can enter through `AI_CUSTOM` with a real sequence, points, and engineer-authored acceptance cases. Cerebras proposes a complete typed graph and can repair it against the immutable oracle. Pinned plant-library jobs take a narrower path: the coding model may propose only a complete replacement parameter object against the controller's authoritative schema, never rewrite the expanded topology. Both routes create immutable child runs and pass deterministic validation, testing, evidence, and approval. This is not a substitute for a qualified equipment pack or field evidence. |
| Contractor review package | Product-wired | Every run emits a signed point map, Brick/tag manifest, alarm, schedule and history requirements, graphics model/PX plan, engineering summary, coverage ledger, source uploads, target artifact, and test evidence. The target is a `.bog` when the graph has a qualified stock/custom lowering, or an exact ProgramObject source-and-wiring package when licensed Workbench compilation is still required. Versioned shop requirements validate alarm policy, exact site/equipment ORDs, IANA-timezone weekly periods, history retention, graphics point references, and station-template mode. Schedules and fixed-interval histories are target-compiled in the BOG lane; alarm/tag installers, contractor-template PX, and optional single-program station assembly are generated/contract-tested but still runtime-gated. Holiday exceptions, COV histories, PX navigation installation, source-package Workbench compilation, whole-project station assembly, and licensed execution remain explicit blockers. |
| Whole-building model | Product-wired build/export | `ProjectSpec` validates topology, builds and tests every equipment program, executes typed cross-equipment signal bindings in topological scan order, requires independent multi-phase project acceptance cases, binds all evidence into project approval, and emits one deterministic bundle. An uploaded contractor BOG receives every program and exact inter-program Niagara link atomically; high-fidelity whole-building physics and licensed runtime execution remain. |
| Capability packs | Prototype / qualifying | Bounded VAV-reheat, AHU safety/cooling, AHU duct-static PI, exhaust-fan proof, two-pump availability selection, contractor-supplied typed graphs, and the 38-model LBNL plant-controller job lane exist. No pack is field-qualified or production-supported. |
| pybog | Target-compiled | Every successful stock/custom-lowered run emits a `.bog`; analyzer/comparator support template review. Stateful plant-library runs deliberately emit ProgramObject source packages instead. Five pinned upstream examples covering AHU PID, chiller, pump rotation, G36 reset, and scheduling compile in the evidence suite. Licensed Niagara import/runtime matrices remain mandatory. |
| Modelica Buildings/OBC | Audited reference source / source-target-complete subset | v13.0.0 pinned. The production-scope audit covers all 116 non-validation models: 28 lower completely to exact typed IR, nine have a stock-Niagara target, the other 19 produce complete deterministic ProgramObject source packages, 84 are explicitly waiting for job-specific design parameters, four remain blocked from the exact lane only by `pre()` event iteration, and zero currently fail translation/elaboration. Configured controllers with a complete target can enter the contractor workflow through `LBNL_G36_CONTROLLER`; exact interface templates are downloadable, independent tests must observe every output, and target-incomplete controllers fail closed during planning. Each `pre()` model has an explicit non-equivalent `host_tick_v1` sampled-scan package when that deployment profile is knowingly selected. Fixed component arrays, indexed root boundaries, Boolean/real vector filters and replicators, reductions, matrix gains, and limiters are scalarized with strict dimension/mask validation. Thirty-one parent CXF documents pass OCE validation; the supply-temperature reset is separately and transparently admitted through a source-bound reviewed `TrimAndRespond` composite. Conservative normalization removes only guard-proven inactive structure, evaluates a restricted grounded Boolean/enum expression language, repairs exact `Assert` string expressions, and leaves arbitrary expressions fail-closed. Public root parameter schemas are discoverable; missing enum metadata is recovered only from exact pinned source declarations; and typed scalar/array/matrix job values are bound before translation, execution, or ProgramObject packaging. A separate full-source audit also includes all 125 validation fixtures. SupplySignals passes through a complete source-package contractor run with one exact PID ProgramObject. Source-package completeness is not licensed Niagara runtime or field qualification. |
| LBNL ctrl-flow | Product-wired design-to-programming-brief boundary | The pinned upstream Linkage Schema contains three HVAC templates, two system families, and 2,593 typed options. BACTalk invokes ctrl-flow's own `ConfigContext` and display mapper, validates choices in dependency order, preserves the valid reciprocal draw-through/blow-through fan dependency, rejects invalid/hidden and out-of-set selections, retains evaluated values, and hashes accepted selections. The programming-brief API binds each template to its exact LBNL G36 controller and expands the design into selected components, 17–38 point obligations, 12–19 normal/fault/recovery scenarios, capability candidates, deliverables, and release blockers. A point-reconciliation API maps only exact names/audited aliases, requires compatible roles/types/units, identifies explicit unit conversion, preserves unmatched contractor points, and blocks missing, ambiguous, or duplicate I/O. A sequence-reconciliation API maps every selected scenario into deterministic facets, retains source hashes and bounded excerpt evidence, exposes missing safety/degraded/recovery language, resists topic-only false positives, and fails closed if a future scenario has no coverage rule. It also emits unapproved structured candidates for comparisons, normalized units and durations, temporal relation, output values, actions, policies, and condition/duration/action clause relationships; point candidates, ambiguities, incompatible design references, and vague language remain visible. The approver-only review API re-derives the upload, verifies the candidate digest and exhaustive decisions, requires explicit ambiguous-point choices, and emits non-executable oracle drafts; vague-language resolution requires a revised source and reinspection. Completed reviews retain the exact source bytes, selected configuration, reviewer identity, and full result in append-only local records; source, candidate, decision, and full-artifact digests are rechecked on retrieval, and authenticated listing/get operations are tenant-filtered. This is local tamper evidence, not external immutable retention. The UI exposes the same item-by-item review with nothing pre-approved. Even successful review leaves graph and deployment gates closed pending independent acceptance-trajectory approval. A retained matrix passes all 44 exposed first-order variants, every generated required-point and scenario contract, and both fan-dependency key orders. This is rigorous engineering planning, not automatic Niagara lowering: independent executable oracle approval, controller parameterization, exact target completion, more upstream templates, and licensed-runtime qualification remain. |
| Modelica Buildings plant controls | Product-wired / source-target complete | The pinned v13.0.0 checkout exposes 73 `Templates.Plants.Controls` source models. All 38 non-validation controls pass the configured product audit and can enter the contractor `JobSpec` workflow through `LBNL_PLANT_CONTROLLER`: exact interface points, an independent acceptance oracle, typed expansion, test report, signed target, review, approval, and export. Stock-only graphs emit `.bog`; stateful graphs emit a deterministic ProgramObject source package with slots, complete topology, and wiring plan. AI change requests are constrained to schema-valid parameters and rebuild/retest a child candidate. Coverage spans enabling, heat pumps, heat-recovery chillers, minimum flow, pumps, setpoints, staging/rotation, and utilities. The complete air-to-water parent supports heating-only, cooling-only, reversible, primary-only, primary-secondary, headered, dedicated, fixed/alternate staging, and optional sidestream-HRC configurations. `make plant-job-contract` proves a 25-point contractor job through its 480-block/715-link graph, assertions over all 12 outputs, 59-ProgramObject package, pre-approval export denial, and immutable signed export. A dual-loop HRC configuration reaches about 1,200 typed blocks and 143 ProgramObjects. Strict array, staging-matrix, parameter, and topology checks reject incoherent configurations. `make plant-controls-contract` and `make plant-controls-audit` retain executable evidence with 38/38 proven and zero hidden pipeline gaps. Licensed Niagara compilation/runtime, controller hardware, and field qualification remain explicit external gates. |
| modelica-json | Verified, locally patched adapter | Exact source revision and the local patch digest are pinned. `make cdl-oce-contract` exercises both the G36 CDL path and bounded extraction of an annotated controls instance from a containing Modelica model. The patch prevents a null component type from crashing recursive model traversal and normalizes insignificant whitespace before symbolic-array connector IRIs are constructed; this unlocks plant models using expressions such as `outPort[nSta + 1]` without changing equations. A full configured VAV container still exceeds the bounded translation window, so this path is not represented as general Modelica elaboration. |
| Rumoca | Product-wired flattener | Apache-2.0 revision `7ddbadf` is source-built and exposed through the allowlisted G36 flatten API. It resolves, instantiates, and emits 125 variables / 76 equations for G36 SupplySignals against pinned MSL 4.1.0. The full multizone VAV controller currently stops at Rumoca's parameter-derived conditional-component instantiation, and Rumoca flat IR is not yet lowered to BACTalk IR. |
| Modelica Standard Library | Verified build input | BSD-3-Clause MSL 4.1.0 is pinned as Rumoca's reproducible dependency. It supplies language types and utilities, not equipment control sequences. |
| Open Control Engine | Verified oracle | 133 public blocks, including temporal blocks, and selected G36 CXF fixtures are catalogued. The isolated runner executes bounded typed/stateful G36 trajectories and captures output evidence. Its `Pre` behavior is explicitly HostTick v1, not Modelica event iteration; BACTalk mirrors that only through an opt-in profile and leaves exact lowering blocked. It is pre-1.0 and not the Niagara compiler. |
| Open Control Library | Verified IR / target-compiled subset | Exact pinned revision contains 137 fault routines across 14 equipment families. All 137 translate and pass upstream vectors in BACTalk; 113 produce stock-block `.bog` artifacts. The routines are commissioning/FDD logic, not complete equipment sequences. |
| AixOCAT | Product-wired / target-compiled subset | A sparse MIT source checkout exposes 211 typed TwinCAT/IEC 61131-3 patterns spanning control primitives, interlocks, sensors, heat-pump checks, and hydronic/pump/valve examples while excluding bundled vendor binaries. Six exact source-bound adapters—scaling, manual override, deadband, dead zone, timed interlock, and hydronic heating curve—pass behavioral vectors and compile to Niagara `.bog`; the remaining 205 are explicitly source-only. |
| Niagara HVAC ProgramObjects | Product-wired reference library | Ben Bartling's pinned MIT library contributes 20 `.bog` archives and 36 ProgramObjects for G36, plant, scheduling, FDD, and optimization. Source/dependency inspection is live; modified source requires licensed Workbench recompilation and runtime qualification. |
| Exact ProgramObject code generation | Product-wired source boundary | CDL `PID`, `PIDWithReset`, `Hysteresis`, `TrueDelay`, `TrueFalseHold`, `Latch`, `Timer`, `MovingAverage`, `FallingEdge`, `Sampler`, `TriggeredSampler`, `UnitDelay`, warning-level `Assert`, plant first-scan `Initialization`, plant `TimerWithReset`, and both reviewed common `TrimAndRespond` variants produce deterministic ProgramObject source, declared Niagara slots, immutable parameters, provenance, and dependency-free Java kernels. The resettable timer preserves input/reset rising edges, elapsed-time restart, falling-edge clear, and threshold assertion. The `have_hol=true` recurrence includes the typed hold signal, minimum duration, and sample-boundary release and matches the expanded 44-block pinned source in OCE over 17 points. `Pre` is available only through the explicit `host_tick_v1` sampled-scan profile and is labeled as non-equivalent to Modelica same-time event iteration. PID matches the independent 10-sample OCE PI-reset golden within `5e-16`; the remaining temporal kernels match explicit OCE/source trajectories. Semantic-contract stamps reject similarly shaped generic blocks. Package generation fails closed when any target exactness blocker lacks a generator/profile. BACTalk does not claim a Niagara binary: licensed Workbench compilation plus runtime/timing parity remain required. |
| BuildingMOTIF | Product-wired | v0.4.0 is isolated under Python 3.11; 115 G36 templates are catalogued and a real VAV template expansion is tested through the product adapter. Templates are semantic topology, not control logic. |
| Brick | Product-wired | Every run emits Turtle and validates the generated subgraph. Current run artifacts remain one equipment object; `ProjectSpec` is the migration path. |
| Haxall/Xeto + Phable | Product-wired | Haxall 4.0.4 validates a known-good G36 graph and rejects a semantic mutation; the API adapter is live. Automatic Brick↔Xeto mapping remains. |
| PNNL ConStrain | Product-wired | Isolated v0.8.0 runtime runs positive/negative G36 time-series cases. Pack-specific applicability and mapping remain. |
| Open-FDD | Product-wired | v4.4.1 rule execution is wired to the API with positive/negative cases. Operational gates and role mappings must be pack-specific. |
| BOPTEST | Product-wired local building-physics runtime and signed run gate | The exact pinned source runs locally under Colima/Docker. Digest-pinned Redis 7.4, MinIO server/client, native-arm web/provision services, and the amd64 PyFMI worker start together. The lifecycle smoke selects and initializes `bestest_air`, discovers 8 inputs and 31 measurements, advances the FMU, reads KPIs, and stops cleanly. A retained ordinary contractor job now passes the full service lane: deterministic acceptance test, Niagara `.bog`, denied pre-approval export, complete typed graph-to-FMU map, bounded overrides, four live 300-second intervals, changing room temperature, real KPIs, zero-error pyfunnel oracle, simulation evidence added to the artifact digest/review bundle, and approved export under an explicitly test-only identity. The API supports this append-once qualification and evidence retrieval; a failed oracle changes the run to failed and blocks approval. Authoritative mappings and long-horizon scenarios for production packs, failure minimization, BACnet attachment, actual human review, and licensed Niagara execution remain explicit gates. |
| Alfalfa | Installed source/client contract | v1.0.0 server source and client execute locally at the API-contract level. Docker/Compose and container dependency/SBOM review block a real runtime. |
| LBNL DFLEXLIBS | Executable reference catalog | v0.2.0 is pinned; all source compiles and selected setpoint-shed plus zone-qualification/hands-off gates execute. It is alpha demand-flexibility research code, not a general equipment sequence library or Niagara pack. |
| pyfunnel | Executable | v2.0.2 runs a real tolerance comparison. It must be retained in every applicable run report. |
| BACpypes3 / BAC0 fake building | Product-wired | Every scanned run emits loopback-only virtual devices for analog, binary, and multistate input/output/value objects, a point map, BAC0 probe, and acceptance runner. Real UDP reads/writes, sensor injection, command priority writes, and automatic result capture are verified. Exact MS/TP serial/router behavior and physical Niagara execution remain hardware-in-loop gates. |
| Independent BACnet Simulator | Product-wired | The pinned MIT `quentinnippert/bacnet-simulator` revision passes 66 selected upstream unit/API/persistence tests. Every scanned run emits a second compatible loopback device set plus launcher; the cross-project contract proves no device/object loss and exposes priority-array, relinquish, COV, scenario, offline-recovery, and REST-observation capabilities. The beta simulator is not BTL-certified and does not provide BACnet/SC, routing/BBMD, native alarm notification, exact MS/TP behavior, or licensed Niagara qualification. |
| VOLTTRON | Product-wired read-only | Scanned jobs emit observation-only driver files, validated in the isolated VOLTTRON environment. Linux runtime smoke remains. |
| Cerebras role router | Product-wired proposal-only | GPT-OSS is isolated to contractor conversation/routing and Qwen 3.8 27B is isolated to graph drafting/repair. `make cerebras-smoke` verifies model entitlements, strict schemas, a real compiled/tested custom graph, and a non-mutating chat response. Proposals create immutable child candidates; deterministic validation cannot be bypassed. |
| Safety kernel | Implemented, transport-free | Default-deny leases, artifact binding, bounds, freshness, slew, and emergency stop exist. There is no live-write network transport. |
| Niagara runtime | Discovered blocker | A licensed isolated Niagara lab, supported-version fixtures, import/compile/restart evidence, and contractor acceptance are required. |

Run `make suite-contract` to execute the selected local stack and retain `.bactalk/integration-evidence.json`. The 2026-09-20 evidence contains 30 passing lanes, including the end-to-end plant contractor job. A passing suite proves reproducible development integration only; it does not claim field or production qualification.
