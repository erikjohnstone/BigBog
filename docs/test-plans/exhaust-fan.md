# Test plan: Exhaust fan (BACTalk standard sequence)

Sequence `exhaust-fan` (Tier 4), requirements version 1.0 (N10 draft, awaiting Gate G-ENG), digest `493055242b4a439f57bfc17638bdf0b762aa19409ed3b9ee3a0fc0a687d7469f`.
Requirements approval (Gate G-ENG): **unapproved**.
Test plan digest `0baf3eb678a8376653b4b77e0b2677ec3f4406f9ccb152bfb15396018f777399`, generated by `bactalk.protocol-test-author/v1` from the requirements alone.

Sources: BACTalk standard sequence: exhaust fan, designer practice; not a Guideline 36 sequence.

## Adequacy

- Accepted: **no**
- Scenarios: 11 run, 0 failed
- Decision coverage: 100.0 % (4 of 4 outcomes)
- Invariants: 2 checked on 240000 steps of 10000 randomised sequences, 0 violations
- Shadow Runtime vs interpreter (D3): scan leg agrees (scan-module-tick, scan bands, 11 cases); default leg agrees (default, default bands, 11 cases)
- Mutation: 78.6 % of 14 sampled mutants caught (target 95 %)

## Requirements and scenarios

### R-01 Fan runs when enabled

The fan is commanded on whenever the enable input is true and off when it is false, with no delay.

Source: BACTalk standard sequence: exhaust fan §1 fan control (designer).

Expected: yFan = True. Otherwise: yFan = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-01 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-01 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-01 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-02 Proof-of-flow alarm

When the fan is commanded on and its proof input stays false for 60 seconds, the proof alarm is raised; it clears as soon as the fan proves or the command is removed.

Source: BACTalk standard sequence: exhaust fan §3 alarms (designer).

Expected: yFanAla = True. Otherwise: yFanAla = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-02 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-02 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-02 timing_before | timing_before | conditions held one scan short of the 60.0 s delay; the outputs must not have changed yet | pass |
| R-02 timing_after | timing_after | conditions held exactly 60.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-02 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-03 Disable stops everything

When the enable input is false the fan command and the proof alarm are all off in the same scan.

Source: BACTalk standard sequence: exhaust fan §1 fan control (designer).

Expected: yFan = False; yFanAla = False. Otherwise: yFan = True.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-03 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-03 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-03 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

## Invariants

- **I-01** Nothing runs while disabled when uEna eq False: yFan = False (BACTalk standard sequence: exhaust fan §1 fan control)
- **I-02** The proof alarm implies a fan command when yFanAla eq True: yFan = True (BACTalk standard sequence: exhaust fan §3 alarms)

## Gaps the test author could not close

- R-01 (failure_behaviour): what should the outputs do when uEna stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-02 (failure_behaviour): what should the outputs do when uFanSta stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-03 (failure_behaviour): what should the outputs do when uEna stuck, stale? No behaviour is stated, so no failure scenario asserts it.
