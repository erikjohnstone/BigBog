# Test plan: Air-source heat pump, single zone with auxiliary heat (BACTalk standard sequence)

Sequence `heat-pump-aux` (Tier 4), requirements version 1.0 (N10 draft, awaiting Gate G-ENG), digest `be093372e98bb90ae5ade537b90f330413006eeecece90f52b51ab1ba45cc7ec`.
Requirements approval (Gate G-ENG): **unapproved**.
Test plan digest `05eb71c80ab084180e68801bb6e7ccc8d203ba45e0dc545ae7138626f4cab398`, generated by `bactalk.protocol-test-author/v1` from the requirements alone.

Sources: BACTalk standard sequence: air-source heat pump, designer practice; not a Guideline 36 sequence.

## Adequacy

- Accepted: **no**
- Scenarios: 63 run, 0 failed
- Decision coverage: 100.0 % (34 of 34 outcomes)
- Invariants: 4 checked on 240000 steps of 10000 randomised sequences, 0 violations
- Shadow Runtime vs interpreter (D3): scan leg agrees (scan-module-tick, scan bands, 63 cases); default leg agrees (default, default bands, 63 cases)
- Mutation: 82.0 % of 100 sampled mutants caught (target 95 %)

## Requirements and scenarios

### R-01 Indoor fan runs when occupied

The indoor fan is commanded on whenever the unit is occupied and off when unoccupied.

Source: BACTalk standard sequence: air-source heat pump §1 fan (designer).

Expected: yFan = True. Otherwise: yFan = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-01 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-01 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-01 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-02 Fan proof alarm

When the fan is commanded on and not proven for 60 seconds the fan proof alarm is raised; it clears when the fan proves or the command is removed.

Source: BACTalk standard sequence: air-source heat pump §6 alarms (designer).

Expected: yFanAla = True. Otherwise: yFanAla = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-02 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-02 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-02 timing_before | timing_before | conditions held one scan short of the 60.0 s delay; the outputs must not have changed yet | pass |
| R-02 timing_after | timing_after | conditions held exactly 60.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-02 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-03 Cooling: compressor with the reversing valve in cooling

With the fan proven and the cooling setpoint at its nominal 297.15 K, the compressor runs in cooling (reversing valve energised) when the zone temperature exceeds the cooling setpoint by more than 0.5 K for 2 minutes, and stops when it falls 0.5 K below the setpoint (1 K deadband). The stage is compared with the live setpoint.

Source: BACTalk standard sequence: air-source heat pump §2 cooling (designer).

Expected: yCom = True; yRev = True. Otherwise: yCom = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-03 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-03 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-03 negative_single [uFanSta] | negative_single | all conditions hold except uFanSta; the outputs must not follow | pass |
| R-03 negative_single [TZon] | negative_single | all conditions hold except TZon; the outputs must not follow | pass |
| R-03 timing_before | timing_before | conditions held one scan short of the 120.0 s delay; the outputs must not have changed yet | pass |
| R-03 timing_after | timing_after | conditions held exactly 120.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-03 boundary_false [TZon at threshold] | boundary_false | TZon at threshold (297.65); the condition does not hold | pass |
| R-03 boundary_true [TZon just above] | boundary_true | TZon just above (297.75); the condition holds | pass |
| R-03 deadband_hold [TZon] | deadband_hold | TZon falls back inside the 1 deadband; the outputs persist | pass |
| R-03 deadband_release [TZon] | deadband_release | TZon passes the far side of the deadband; the outputs release | pass |
| R-03 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-04 Heating: compressor with the reversing valve in heating

With the fan proven, the outdoor air above the 268.15 K (−5 °C) compressor lockout (locked again below 267.15 K, a 1 K deadband) and the heating setpoint at its nominal 293.15 K, the compressor runs in heating (reversing valve de-energised) when the zone temperature falls more than 0.5 K below the heating setpoint for 2 minutes, and stops when it rises 0.5 K above the setpoint.

Source: BACTalk standard sequence: air-source heat pump §3 heating (designer).

Expected: yCom = True; yRev = False; yAux = False. Otherwise: yCom = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-04 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-04 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-04 negative_single [uFanSta] | negative_single | all conditions hold except uFanSta; the outputs must not follow | pass |
| R-04 negative_single [TOut] | negative_single | all conditions hold except TOut; the outputs must not follow | pass |
| R-04 negative_single [TZon] | negative_single | all conditions hold except TZon; the outputs must not follow | pass |
| R-04 timing_before | timing_before | conditions held one scan short of the 120.0 s delay; the outputs must not have changed yet | pass |
| R-04 timing_after | timing_after | conditions held exactly 120.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-04 boundary_false [TOut at threshold] | boundary_false | TOut at threshold (268.15); the condition does not hold | pass |
| R-04 boundary_true [TOut just above] | boundary_true | TOut just above (268.25); the condition holds | pass |
| R-04 deadband_hold [TOut] | deadband_hold | TOut falls back inside the 1 deadband; the outputs persist | pass |
| R-04 deadband_release [TOut] | deadband_release | TOut passes the far side of the deadband; the outputs release | pass |
| R-04 boundary_false [TZon at threshold] | boundary_false | TZon at threshold (292.65); the condition does not hold | pass |
| R-04 boundary_true [TZon just below] | boundary_true | TZon just below (292.55); the condition holds | pass |
| R-04 deadband_hold [TZon] | deadband_hold | TZon falls back inside the 1 deadband; the outputs persist | pass |
| R-04 deadband_release [TZon] | deadband_release | TZon passes the far side of the deadband; the outputs release | pass |
| R-04 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-05 Unoccupied shuts the unit down

When unoccupied the fan, the compressor, the auxiliary heat and the fan proof alarm are off in the same scan.

Source: BACTalk standard sequence: air-source heat pump §1 fan (designer).

Expected: yFan = False; yCom = False; yFanAla = False; yAux = False. Otherwise: yFan = True.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-05 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-05 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-05 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-06 Auxiliary heat below the compressor lockout

With the fan proven, the outdoor air below 267.15 K (the compressor lockout less its 1 K deadband; the compressor is allowed again above 268.15 K) and a heating demand (zone more than 0.5 K below the heating setpoint) for 2 minutes, the auxiliary electric heat runs alone: the compressor stays off.

Source: BACTalk standard sequence: air-source heat pump §4 auxiliary heat lockout (designer).

Expected: yAux = True; yCom = False. Otherwise: yAux = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-06 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-06 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-06 negative_single [uFanSta] | negative_single | all conditions hold except uFanSta; the outputs must not follow | pass |
| R-06 negative_single [TOut] | negative_single | all conditions hold except TOut; the outputs must not follow | pass |
| R-06 negative_single [TZon] | negative_single | all conditions hold except TZon; the outputs must not follow | pass |
| R-06 timing_before | timing_before | conditions held one scan short of the 120.0 s delay; the outputs must not have changed yet | pass |
| R-06 timing_after | timing_after | conditions held exactly 120.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-06 boundary_false [TOut at threshold] | boundary_false | TOut at threshold (267.15); the condition does not hold | pass |
| R-06 boundary_true [TOut just below] | boundary_true | TOut just below (267.05); the condition holds | pass |
| R-06 deadband_hold [TOut] | deadband_hold | TOut falls back inside the 1 deadband; the outputs persist | pass |
| R-06 deadband_release [TOut] | deadband_release | TOut passes the far side of the deadband; the outputs release | pass |
| R-06 boundary_false [TZon at threshold] | boundary_false | TZon at threshold (292.65); the condition does not hold | pass |
| R-06 boundary_true [TZon just below] | boundary_true | TZon just below (292.55); the condition holds | pass |
| R-06 deadband_hold [TZon] | deadband_hold | TZon falls back inside the 1 deadband; the outputs persist | pass |
| R-06 deadband_release [TZon] | deadband_release | TZon passes the far side of the deadband; the outputs release | pass |
| R-06 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-07 Supplemental heat when the compressor cannot hold

When a heating demand persists with the zone more than 2 K below the heating setpoint (1 K deadband) for 10 minutes from the start of the demand, the auxiliary heat is added to the compressor; it drops out when the zone is within 1 K of the setpoint.

Source: BACTalk standard sequence: air-source heat pump §4 auxiliary heat supplemental (designer).

Expected: yAux = True; yCom = True. Otherwise: yAux = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-07 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-07 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-07 timing_before | timing_before | conditions held one scan short of the 600.0 s delay; the outputs must not have changed yet | pass |
| R-07 timing_after | timing_after | conditions held exactly 600.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-07 boundary_false [TZon at threshold] | boundary_false | TZon at threshold (291.15); the condition does not hold | pass |
| R-07 boundary_true [TZon just below] | boundary_true | TZon just below (291.05); the condition holds | pass |
| R-07 deadband_hold [TZon] | deadband_hold | TZon falls back inside the 1 deadband; the outputs persist | pass |
| R-07 deadband_release [TZon] | deadband_release | TZon passes the far side of the deadband; the outputs release | pass |
| R-07 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

## Invariants

- **I-01** Nothing runs while unoccupied when uOcc eq False: yFan = False; yCom = False; yAux = False (BACTalk standard sequence: air-source heat pump §1 fan)
- **I-02** No compressor heating below the lockout when TOut lt 267.15 and yRev eq False: yCom = False (BACTalk standard sequence: air-source heat pump §3 heating)
- **I-03** No compressor without a proven fan when uFanSta eq False: yCom = False; yAux = False (BACTalk standard sequence: air-source heat pump §2 cooling)
- **I-04** Auxiliary heat only in heating mode when yAux eq True: yRev = False (BACTalk standard sequence: air-source heat pump §4 auxiliary heat)

## Gaps the test author could not close

- R-01 (failure_behaviour): what should the outputs do when uOcc stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-02 (failure_behaviour): what should the outputs do when uFanSta stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-03 (failure_behaviour): what should the outputs do when uFanSta stuck, stale; TZon stuck, stale, out of range low, out of range high? No behaviour is stated, so no failure scenario asserts it.
- R-04 (failure_behaviour): what should the outputs do when uFanSta stuck, stale; TOut stuck, stale, out of range low, out of range high; TZon stuck, stale, out of range low, out of range high? No behaviour is stated, so no failure scenario asserts it.
- R-05 (failure_behaviour): what should the outputs do when uOcc stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-06 (failure_behaviour): what should the outputs do when uFanSta stuck, stale; TOut stuck, stale, out of range low, out of range high; TZon stuck, stale, out of range low, out of range high? No behaviour is stated, so no failure scenario asserts it.
- R-07 (failure_behaviour): what should the outputs do when TZon stuck, stale, out of range low, out of range high? No behaviour is stated, so no failure scenario asserts it.
