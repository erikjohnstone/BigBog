# Test plan: Duty/standby pump pair (BACTalk standard sequence)

Sequence `pump-duty-standby` (Tier 4), requirements version 1.0 (N10 draft, awaiting Gate G-ENG), digest `9464c84686cb29aa63ee8cf62a0e75e67fba7135b73aeb2086f6d91dbdd1ed58`.
Requirements approval (Gate G-ENG): **unapproved**.
Test plan digest `ee044af33999f9ebfa7df5566d726edce5482488d95d0b11c54f5d494febc2a9`, generated by `bactalk.protocol-test-author/v1` from the requirements alone.

Sources: BACTalk standard sequence: duty/standby pump pair, designer practice; not a Guideline 36 sequence.

## Adequacy

- Accepted: **no**
- Scenarios: 34 run, 0 failed
- Decision coverage: 100.0 % (40 of 40 outcomes)
- Invariants: 4 checked on 240000 steps of 10000 randomised sequences, 0 violations
- Shadow Runtime vs interpreter (D3): scan leg agrees (scan-module-tick, scan bands, 34 cases); default leg agrees (default, default bands, 34 cases)
- Mutation: 89.9 % of 99 sampled mutants caught (target 95 %)

## Requirements and scenarios

### R-01 Lead pump runs when enabled

With the system enabled, the selected lead pump (pump 1 unless pump 2 is selected) is commanded on when it is available; the standby pump stays off.

Source: BACTalk standard sequence: duty/standby pump pair §1 duty selection (designer).

Expected: yPum1 = True. Otherwise: yPum1 = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-01 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-01 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-01 negative_single [uEna] | negative_single | all conditions hold except uEna; the outputs must not follow | pass |
| R-01 negative_single [uLead2] | negative_single | all conditions hold except uLead2; the outputs must not follow | pass |
| R-01 negative_single [uPum1Ava] | negative_single | all conditions hold except uPum1Ava; the outputs must not follow | pass |
| R-01 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-02 Pump 2 leads when selected

With the system enabled and pump 2 selected as lead and available, pump 2 is commanded on and pump 1 stays off.

Source: BACTalk standard sequence: duty/standby pump pair §1 duty selection (designer).

Expected: yPum2 = True; yPum1 = False. Otherwise: yPum2 = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-02 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-02 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-02 negative_single [uEna] | negative_single | all conditions hold except uEna; the outputs must not follow | pass |
| R-02 negative_single [uLead2] | negative_single | all conditions hold except uLead2; the outputs must not follow | pass |
| R-02 negative_single [uPum2Ava] | negative_single | all conditions hold except uPum2Ava; the outputs must not follow | pass |
| R-02 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-03 Standby takes over an unavailable lead

With pump 1 leading, when pump 1 becomes unavailable the standby pump 2 is commanded on immediately and pump 1 is commanded off.

Source: BACTalk standard sequence: duty/standby pump pair §2 failover (designer).

Expected: yPum2 = True; yPum1 = False. Otherwise: yPum1 = True; yPum2 = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-03 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-03 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-03 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-04 No pump available alarm

When the system is enabled and neither pump is available, both pumps are commanded off and the no-pump-available alarm is raised; it clears when a pump becomes available or the system is disabled.

Source: BACTalk standard sequence: duty/standby pump pair §3 alarms (designer).

Expected: yNoPumAla = True; yPum1 = False; yPum2 = False. Otherwise: yNoPumAla = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-04 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-04 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-04 negative_single [uEna] | negative_single | all conditions hold except uEna; the outputs must not follow | pass |
| R-04 negative_single [uPum1Ava] | negative_single | all conditions hold except uPum1Ava; the outputs must not follow | pass |
| R-04 negative_single [uPum2Ava] | negative_single | all conditions hold except uPum2Ava; the outputs must not follow | pass |
| R-04 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-05 Pump failure alarm

When a commanded pump is not proven for 30 seconds the pump failure alarm is raised; it clears when the pump proves or its command is removed.

Source: BACTalk standard sequence: duty/standby pump pair §3 alarms (designer).

Expected: yPumFaiAla = True. Otherwise: yPumFaiAla = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-05 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-05 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-05 timing_before | timing_before | conditions held one scan short of the 30.0 s delay; the outputs must not have changed yet | pass |
| R-05 timing_after | timing_after | conditions held exactly 30.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-05 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-06 Disable stops both pumps

When the system is disabled both pumps and both alarms are off in the same scan.

Source: BACTalk standard sequence: duty/standby pump pair §1 duty selection (designer).

Expected: yPum1 = False; yPum2 = False; yNoPumAla = False; yPumFaiAla = False. Otherwise: yPum1 = True.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-06 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-06 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-06 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-07 Standby pump failure alarm

With pump 2 leading, when pump 2 is commanded and not proven for 30 seconds the pump failure alarm is raised; it clears when the pump proves or its command is removed.

Source: BACTalk standard sequence: duty/standby pump pair §3 alarms (designer).

Expected: yPumFaiAla = True. Otherwise: yPumFaiAla = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-07 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-07 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-07 timing_before | timing_before | conditions held one scan short of the 30.0 s delay; the outputs must not have changed yet | pass |
| R-07 timing_after | timing_after | conditions held exactly 30.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-07 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

## Invariants

- **I-01** Nothing runs while disabled when uEna eq False: yPum1 = False; yPum2 = False; yNoPumAla = False (BACTalk standard sequence: duty/standby pump pair §1 duty selection)
- **I-02** An unavailable pump is never commanded when uPum1Ava eq False: yPum1 = False (BACTalk standard sequence: duty/standby pump pair §2 failover)
- **I-03** An unavailable pump is never commanded when uPum2Ava eq False: yPum2 = False (BACTalk standard sequence: duty/standby pump pair §2 failover)
- **I-04** The no-pump alarm implies both pumps off when yNoPumAla eq True: yPum1 = False; yPum2 = False (BACTalk standard sequence: duty/standby pump pair §3 alarms)

## Gaps the test author could not close

- R-01 (failure_behaviour): what should the outputs do when uEna stuck, stale; uLead2 stuck, stale; uPum1Ava stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-02 (failure_behaviour): what should the outputs do when uEna stuck, stale; uLead2 stuck, stale; uPum2Ava stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-03 (failure_behaviour): what should the outputs do when uPum1Ava stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-04 (failure_behaviour): what should the outputs do when uEna stuck, stale; uPum1Ava stuck, stale; uPum2Ava stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-05 (failure_behaviour): what should the outputs do when uPum1Sta stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-06 (failure_behaviour): what should the outputs do when uEna stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-07 (failure_behaviour): what should the outputs do when uPum2Sta stuck, stale? No behaviour is stated, so no failure scenario asserts it.
