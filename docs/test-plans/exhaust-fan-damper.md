# Test plan: Exhaust fan with isolation damper (BACTalk standard sequence)

Sequence `exhaust-fan-damper` (Tier 4), requirements version 1.0 (N10 draft, awaiting Gate G-ENG), digest `7ab4c6fc0fbb41a2f135a8796091f58611d56f6d27f12ad8af8b7106bcd4bb46`.
Requirements approval (Gate G-ENG): **unapproved**.
Test plan digest `078f1a460ba4551d4b592efee4cb3cbc16cd3312b31a590b34b7f640bc47d917`, generated by `bactalk.protocol-test-author/v1` from the requirements alone.

Sources: BACTalk standard sequence: exhaust fan, designer practice; not a Guideline 36 sequence.

## Adequacy

- Accepted: **no**
- Scenarios: 21 run, 0 failed
- Decision coverage: 100.0 % (12 of 12 outcomes)
- Invariants: 2 checked on 240000 steps of 10000 randomised sequences, 0 violations
- Shadow Runtime vs interpreter (D3): scan leg agrees (scan-module-tick, scan bands, 21 cases); default leg agrees (default, default bands, 21 cases)
- Mutation: 86.1 % of 36 sampled mutants caught (target 95 %)

## Requirements and scenarios

### R-01 Fan runs when enabled and the damper proves open

With the enable input true, the fan is commanded on once the isolation damper's open end switch proves; it is commanded off when the enable input is false.

Source: BACTalk standard sequence: exhaust fan §1 fan control isolation damper interlock (designer).

Expected: yFan = True. Otherwise: yFan = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-01 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-01 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-01 negative_single [uEna] | negative_single | all conditions hold except uEna; the outputs must not follow | pass |
| R-01 negative_single [uDamOpe] | negative_single | all conditions hold except uDamOpe; the outputs must not follow | pass |
| R-01 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-02 Proof-of-flow alarm

When the fan is commanded on and its proof input stays false for 60 seconds, the proof alarm is raised; it clears as soon as the fan proves or the command is removed.

Source: BACTalk standard sequence: exhaust fan §3 alarms (designer).

Expected: yFanAla = True. Otherwise: yFanAla = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-02 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-02 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-02 timing_before | timing_before | conditions held one scan short of the 60.0 s delay; the outputs must not have changed yet | pass |
| R-02 timing_after | timing_after | conditions held exactly 60.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-02 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-03 Disable stops everything

When the enable input is false the fan command, the damper command and the proof alarm are all off in the same scan.

Source: BACTalk standard sequence: exhaust fan §1 fan control (designer).

Expected: yFan = False; yFanAla = False; yDam = False. Otherwise: yFan = True.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-03 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-03 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-03 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-04 Fan starts after the damper timeout

If the damper end switch has not proved open 30 seconds after the enable input became true, the fan starts anyway (a failed end switch must not keep the space unventilated).

Source: BACTalk standard sequence: exhaust fan §1 fan control end switch timeout (designer).

Expected: yFan = True. Otherwise: yFan = True.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-04 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-04 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-04 timing_before | timing_before | conditions held one scan short of the 30.0 s delay; the outputs must not have changed yet | pass |
| R-04 timing_after | timing_after | conditions held exactly 30.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-04 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-05 Damper opens when enabled

The isolation damper is commanded open whenever the enable input is true and closed when it is false.

Source: BACTalk standard sequence: exhaust fan §2 isolation damper (designer).

Expected: yDam = True. Otherwise: yDam = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-05 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-05 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-05 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

## Invariants

- **I-01** Nothing runs while disabled when uEna eq False: yFan = False; yDam = False (BACTalk standard sequence: exhaust fan §1 fan control)
- **I-02** The proof alarm implies a fan command when yFanAla eq True: yFan = True (BACTalk standard sequence: exhaust fan §3 alarms)

## Gaps the test author could not close

- R-01 (failure_behaviour): what should the outputs do when uEna stuck, stale; uDamOpe stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-02 (failure_behaviour): what should the outputs do when uFanSta stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-03 (failure_behaviour): what should the outputs do when uEna stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-04 (failure_behaviour): what should the outputs do when uDamOpe stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-05 (failure_behaviour): what should the outputs do when uEna stuck, stale? No behaviour is stated, so no failure scenario asserts it.
