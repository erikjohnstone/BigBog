# Test plan: Dedicated outdoor air system (BACTalk standard sequence)

Sequence `doas` (Tier 4), requirements version 1.0 (N10 draft, awaiting Gate G-ENG), digest `348f4ac273ae950e140b4fdccc379a418994418b16be07578f773258397cb440`.
Requirements approval (Gate G-ENG): **unapproved**.
Test plan digest `4de590062db1ee2a814bfcd0f10d4ac7f2bfade8e46bd1b77586c83018b89181`, generated by `bactalk.protocol-test-author/v1` from the requirements alone.

Sources: BACTalk standard sequence: dedicated outdoor air system, designer practice; not a Guideline 36 sequence.

## Adequacy

- Accepted: **no**
- Scenarios: 38 run, 0 failed
- Decision coverage: 100.0 % (10 of 10 outcomes)
- Invariants: 3 checked on 240000 steps of 10000 randomised sequences, 0 violations
- Shadow Runtime vs interpreter (D3): scan leg agrees (scan-module-tick, scan bands, 38 cases); default leg agrees (default, default bands, 38 cases)
- Mutation: 72.5 % of 69 sampled mutants caught (target 95 %)

## Requirements and scenarios

### R-01 Supply fan runs when occupied

The supply fan is commanded on whenever the unit is occupied and off when unoccupied.

Source: BACTalk standard sequence: dedicated outdoor air system §1 fans (designer).

Expected: ySupFan = True. Otherwise: ySupFan = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-01 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-01 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-01 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-02 Supply fan proof alarm

When the supply fan is commanded on and not proven for 60 seconds the fan proof alarm is raised; it clears when the fan proves or the command is removed.

Source: BACTalk standard sequence: dedicated outdoor air system §5 alarms (designer).

Expected: yFanAla = True. Otherwise: yFanAla = False.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-02 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-02 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-02 timing_before | timing_before | conditions held one scan short of the 60.0 s delay; the outputs must not have changed yet | pass |
| R-02 timing_after | timing_after | conditions held exactly 60.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-02 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-03 Heating coil holds the supply setpoint

With the supply fan proven, the heating coil valve is modulated (proportional, 0.5 per K) to hold the supply air temperature at the setpoint less half the 1 K deadband; with the supply 3 K or more below the nominal 291.15 K setpoint for 60 seconds the valve is at least 90 % open, and it is closed whenever the supply is above the setpoint.

Source: BACTalk standard sequence: dedicated outdoor air system §2 supply temperature heating (designer).

Expected: yHeaVal ≥ 0.9; yCooVal = 0 ±0.001. Otherwise: yHeaVal in [0, 1].

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-03 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-03 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-03 negative_single [uSupFanSta] | negative_single | all conditions hold except uSupFanSta; the outputs must not follow | pass |
| R-03 negative_single [TSup] | negative_single | all conditions hold except TSup; the outputs must not follow | pass |
| R-03 timing_before | timing_before | conditions held one scan short of the 60.0 s delay; the outputs must not have changed yet | pass |
| R-03 timing_after | timing_after | conditions held exactly 60.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-03 boundary_false [TSup at threshold] | boundary_false | TSup at threshold (288.15); the condition does not hold | pass |
| R-03 boundary_true [TSup just below] | boundary_true | TSup just below (288.05); the condition holds | pass |
| R-03 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-04 Cooling coil holds the supply setpoint

With the supply fan proven, the cooling coil valve is modulated (proportional, 0.5 per K) to hold the supply air temperature at the setpoint plus half the deadband; with the supply 3 K or more above the nominal setpoint for 60 seconds the valve is at least 90 % open, and it is closed whenever the supply is below the setpoint.

Source: BACTalk standard sequence: dedicated outdoor air system §2 supply temperature cooling (designer).

Expected: yCooVal ≥ 0.9; yHeaVal = 0 ±0.001. Otherwise: yCooVal in [0, 1].

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-04 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-04 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-04 negative_single [uSupFanSta] | negative_single | all conditions hold except uSupFanSta; the outputs must not follow | pass |
| R-04 negative_single [TSup] | negative_single | all conditions hold except TSup; the outputs must not follow | pass |
| R-04 timing_before | timing_before | conditions held one scan short of the 60.0 s delay; the outputs must not have changed yet | pass |
| R-04 timing_after | timing_after | conditions held exactly 60.0 s plus 1 scans of tolerance; the outputs follow | pass |
| R-04 boundary_false [TSup at threshold] | boundary_false | TSup at threshold (294.15); the condition does not hold | pass |
| R-04 boundary_true [TSup just above] | boundary_true | TSup just above (294.25); the condition holds | pass |
| R-04 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-05 Both valves closed inside the deadband

With the supply fan proven and the supply air temperature inside the 1 K deadband around the nominal setpoint (290.65 K to 291.65 K), both coil valves are closed.

Source: BACTalk standard sequence: dedicated outdoor air system §2 supply temperature deadband (designer).

Expected: yHeaVal = 0 ±0.001; yCooVal = 0 ±0.001. Otherwise: yHeaVal in [0, 1].

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-05 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-05 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-05 negative_single [uSupFanSta] | negative_single | all conditions hold except uSupFanSta; the outputs must not follow | pass |
| R-05 negative_single [TSup] | negative_single | all conditions hold except TSup; the outputs must not follow | pass |
| R-05 boundary_true [TSup at lower edge] | boundary_true | TSup at lower edge (290.65); the condition holds | pass |
| R-05 boundary_false [TSup just below lower edge] | boundary_false | TSup just below lower edge (290.55); the condition does not hold | pass |
| R-05 boundary_true [TSup at upper edge] | boundary_true | TSup at upper edge (291.65); the condition holds | pass |
| R-05 boundary_false [TSup just above upper edge] | boundary_false | TSup just above upper edge (291.75); the condition does not hold | pass |
| R-05 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

### R-06 Unoccupied shuts the unit down

When unoccupied the supply fan and both valves are off in the same scan, and the fan proof alarm is off.

Source: BACTalk standard sequence: dedicated outdoor air system §1 fans (designer).

Expected: ySupFan = False; yHeaVal = 0 ±0.001; yCooVal = 0 ±0.001; yFanAla = False. Otherwise: ySupFan = True.

| Scenario | Kind | Why | Result |
|---|---|---|---|
| R-06 positive | positive | every condition holds for the stated delay; the stated outputs follow | pass |
| R-06 negative | negative | no condition holds for longer than the release window; the 'otherwise' outputs hold | pass |
| R-06 recovery | recovery | after the outputs followed, the conditions clear; the outputs recover | pass |

## Invariants

- **I-01** Nothing runs while unoccupied when uOcc eq False: ySupFan = False; yHeaVal = 0; yCooVal = 0 (BACTalk standard sequence: dedicated outdoor air system §1 fans)
- **I-02** Heating and cooling valves never open together when yHeaVal gt 0.001: yCooVal = 0 ±0.001 (BACTalk standard sequence: dedicated outdoor air system §2 supply temperature)
- **I-03** Valve positions are fractions: yHeaVal in [0, 1]; yCooVal in [0, 1] (BACTalk standard sequence: dedicated outdoor air system §2 supply temperature)

## Gaps the test author could not close

- R-01 (failure_behaviour): what should the outputs do when uOcc stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-02 (failure_behaviour): what should the outputs do when uSupFanSta stuck, stale? No behaviour is stated, so no failure scenario asserts it.
- R-03 (failure_behaviour): what should the outputs do when uSupFanSta stuck, stale; TSup stuck, stale, out of range low, out of range high? No behaviour is stated, so no failure scenario asserts it.
- R-04 (failure_behaviour): what should the outputs do when uSupFanSta stuck, stale; TSup stuck, stale, out of range low, out of range high? No behaviour is stated, so no failure scenario asserts it.
- R-05 (failure_behaviour): what should the outputs do when uSupFanSta stuck, stale; TSup stuck, stale, out of range low, out of range high? No behaviour is stated, so no failure scenario asserts it.
- R-06 (failure_behaviour): what should the outputs do when uOcc stuck, stale? No behaviour is stated, so no failure scenario asserts it.
